__pycache__/
*.py[cod]
.venv/
venv/
.env
.DS_Store
.pytest_cache/

# Impeccable transient state (screenshots, build-phase machine)
.impeccable/review/
.impeccable/build/

# Local Claude tooling (skills, agents) stays out of the published repo
.claude/
.impeccable/
